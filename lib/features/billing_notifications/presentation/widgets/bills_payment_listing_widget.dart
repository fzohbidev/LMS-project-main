// lib/features/billing_notifications/presentation/widgets/bills_payments_listing_widget.dart

import 'package:flutter/material.dart';

class BillsPaymentsListingWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text('List of bills and payments here.'),
        ],
      ),
    );
  }
}
